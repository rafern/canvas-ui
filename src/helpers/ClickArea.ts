/**
 * A 4-tuple containing a clickable area as a rectangle. The 4-tuple contains,
 * respectively, the left coordinate, the right coordinate, the top coordinate
 * and the bottom coordinate.
 *
 * @category Helper
 */
export type ClickArea = [left: number, right: number, top: number, bottom: number];