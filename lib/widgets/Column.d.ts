import { MultiContainer } from './MultiContainer';
import type { Theme } from '../theme/Theme';
/**
 * A vertical {@link MultiContainer}.
 *
 * @category Widget
 */
export declare class Column extends MultiContainer {
    /** Create a new Column. */
    constructor(themeOverride?: Theme | null);
}
